# 更新日志

## v1.6.4

- 修复节点图悬浮详情失焦不消失：tooltip 不拦截鼠标，失焦/鼠标离开页面/滚动时兑底收起
- 流量页支持**同时展开多台节点**的节点图（去单实例限制）
- 卡片视图/排序下拉按钮的三角符号与展开状态联动（▲/▼）
- 移动端导航栏重组：顺序 NODES → 刷新 → 主题切换（单键循环）→ 语言 → 下拉栏（视图/配色/设置/后台收进 ≡ 下拉）
- 刷新按钮图标改为 CSS 绘制圆环箭头，消除字符错位与大小不一致

## v1.6.3

- 修复首次点「详情」整页刷新闪动（根因）：懒加载的节点图组件没有 Suspense 边界，首次展开挂起整棵组件树；且 uPlot 样式随懒分块动态插入导致全页样式重算。修复：图表外包 Suspense（加载只限图表区域）、uPlot CSS 提前到入口（随 index.html 静态 `<link>` 加载）、流量页空闲预取图表分块（首次展开不再下载代码）
- 「详情」右侧三角状态修正：未展开 ◀（朝向文字），展开 ▼（朝下）

## v1.6.2

- 修复第一次打开实例详情页时页面刷新闪动：前进/后退改为**软刷新**（清空查询缓存 + 回顶），不再整页 reload（整页 reload 会被同文档 hash 导航的 popstate 误触发，导致首次打开详情页闪动）；bfcache 恢复同样软刷新
- 首页空闲时预取详情页/流量页代码分块，首次打开不再因下载代码而闪动

## v1.6.1

- 修复节点图每隔几秒重新加载：窗口锚点改为只在换范围/换日期/手动刷新时更新，查询键稳定；实时数据直接**追加**到曲线右端（uPlot setData），不再重建图表；面板新增「⟳ 刷新」按钮重新拉取当前范围

## v1.6.0

- **节点图加载提速**：汇总查询/往期查询/节点图查询恢复共享前端缓存（20s），展开节点图若命中当天已拉取的行数则**零新请求**；刷新按钮改为「清前端缓存 + 重新拉取」，立即生效
- **数据保留改为 14 天**（后端 CF-Server-Monitor）：`/api/history/all` 支持 336h；周轮换时自动清理超过 14 天的旧数据（轮换中断兑底）；主题同步：详情页新增 14 天档、流量汇总日期可选 14 天内、节点图新增 14 天档与日期上限 14 天

## v1.5.8

- 流量页节点图改为**双 Y 轴图**：左轴「当日流量（累计，字节）」实线、右轴「网速（字节/秒）」浅色虚线，图例/悬停四行单位各自对应
- 节点图时间范围联动：1h / 2h / 3h / 7h / 1 天 / 3 天 / 7 天 快捷档；日期选择框（NieR 风格）可选起始日查看更早日期（后端仅保留 7 天，超出时提示；未登录限 24 小时）
- 移动端节点图空图修复：窗口改为「起点 → 当前时刻」按需定档（此前「今天」窗口按整天定 48h 档，未登录被 401 拦截导致空图）；无采样/加载失败/超限均有明确提示与重试

## v1.5.7

- 流量页节点图改为「当日流量（累计）」：同一份采样做梯形积分、按时间轴展示累计上/下行字节，坐标轴/图例/悬停单位同步切换为字节（纯前端计算，零新增请求）

## v1.5.6

- 性能打磨：品牌图标 256px→32px（66KB→2KB）；三档卡片启用 content-visibility（大列表渲染提速）；实例详情页懒加载已有；React Query 默认缓存配置确认无需调整
- 移动端实例详情页：每个块无横向/纵向滚动条（面板 overflow visible、隐藏 WebKit 滚动条、长值断词）
- 修复前进/后退未自动刷新：除 bfcache 恢复外，hash 路由的 popstate 前进/后退也强制整页刷新（不保留旧状态）
- 同步更新 v1.0.0 描述

## v1.5.5

- 浏览器前进/后退从 bfcache 恢复时不保留旧状态：pageshow(persisted) 强制整页刷新，重新拉数据与实时连接
- 移动端底部导航：点击后台等按钮不再粘住与当前页面同色的高亮背景（悬停态仅对真有鼠标的设备生效）

## v1.5.4

- 方案 B 内置图标：`icon.ico` 进 `assets/`（同时转出 icon.png 兼容 Safari），导航栏左侧展示品牌图标（16px、移动端 14px、加载失败自动隐藏），浏览器标签页 favicon 同步内置（后台外观设置配置的站点图标仍可覆盖）
- 移动端顶栏按钮重排序：外观三键 → 语言 → 视图 → 配色 → 刷新 → 设置 → 后台，节点读数最后
- 同步更新 v1.0.0 描述

## v1.5.3

- COLORS 面板的取色框改为**内置取色器**（与项目统一）：预设色板（24 色 NieR 色系）+ HEX 输入，不再调起浏览器原生取色对话框；色块按钮 NieR 风格（方角、描边、▾ 箭头）
- 同步更新 v1.0.0 描述（此后每次发布都保持该描述与当前功能同步）

## v1.5.2

- 移动端 COLORS 面板：移到导航栏下方（留 8px 间隙）、宽度只随内容字段而定不再拉伸占满，超高时可纵向滚动

## v1.5.1

- 流量页加载提速：初始只拉汇总（summary 模式），展开某台节点时按需拉取明细；实时总带宽按 1 秒节拍统一更新（与首页同逻辑）
- 切换页面自动回到顶部
- 移动端流量页：当日流量/采样峰值卡无横向纵向滚动条、内容可换行，不再闪动

## v1.5.0

- 流量页：刷新按钮生效（今日/往期查询绕过前端缓存）；新增实时网速展示（汇总卡实时带宽、表格「实时」列、移动卡片实时行、速率明细图追加实时采样点）
- 移动端设置页：页签栏/文本域/面板滚动条彻底清除（!important + scrollbar-width + 隐藏 WebKit 滚动条）
- COLORS 面板：点击外部或 Esc 自动关闭；关闭后按钮不再残留白色高亮（触屏设备不再粘住 :hover）
- 底部导航「后台」按钮初始化即显示（配置未到时不再隐藏，到达后按开关）

## v1.4.5

- 移动端 COLORS 改为底部抽屉（贴在底部停靠栏上方、最多 42vh），不再覆盖大部分页面
- 修复设置页滚动条未消失的根因：设置页根容器补上 `.theme-manage` 类（此前全部 .theme-manage 移动端规则未命中），面板/页签/文本域彻底无滚动条

## v1.4.4

- 移动端导航：修复设置页时「状态」与「设置」同时高亮（顶部导航与底部停靠栏均按 `?view=theme-manage` 判定）
- 移动端设置页：面板禁止横向滚动条、页签改换行、标题/输入框随屏缩放、视图模式单列

## v1.4.3

- 移动端导航栏重设计：≤720px 隐藏顶部横排导航，改为底部停靠栏（状态/流量/设置/后台，带图形符号与选中反色），深色模式同步配色；内容、页脚、回顶按钮、实时时限提示均向停靠栏让位（含 iOS 安全区）

## v1.4.2

- 移动端适配：窄屏强制单列卡片（覆盖固定列数设置）、顶栏控件压缩换行、配色面板贴边不溢出、总览卡/图表头/设置工具栏/流量汇总在手机上单列或紧凑、480px 以下进一步压缩

## v1.4.1

- 修复 COMPACT / MINI 卡片悬停变黑：拉伸链接不再触发 anchor 悬停反转（透明背景、无下划线）
- Swap 栏百分号：大卡 SWAP 改为百分比显示，迷你卡无 Swap 时也显示 0%
- LIST 表格右上角新增「↺ 重置样式」：恢复默认列宽并恢复全部列可见

## v1.4.0

- COMPACT / MINI 悬停样式与 LARGE 同步（2px 描边 + 4px 硬阴影）；MINI 卡片填满网格单元，大小统一
- 首页排序去掉「价格」维度；流量页新增排序下拉按钮；往期记录改为日历选择（未登录限今天）
- 顶栏：外观改为 LIGHT/SYSTEM/DARK 三个独立按钮；卡片视图改为下拉栏；语言按钮与其他按钮同尺寸
- 暗色背景设置生效：深色画布/面板/悬停底随「黑色程度」用 color-mix 加深
- 三种卡片新增 Swap 栏（大卡进度条、小卡 gauge、迷你卡 metric）
- LARGE 卡片与 LIST 表格去掉到期字段（表格「在线/到期」列改为「运行」）
- 表格 CPU/内存/磁盘/负载/网络列去掉固定最大宽，随列宽拖拽同步拉伸

## v1.3.0

- 卡片布局设置：网格列数（自动/1–6 列，大/小/迷你统一）；分组顺序拖动排序；LIST 表格列可见性开关（9 列）
- COMPACT / MINI 整卡可点击打开详情（与 LARGE 统一）；名称悬停样式与 LARGE 一致（不反转背景）；大卡/迷你卡名称换行（两行超出省略）；三种卡片网格行等高、大小统一
- 去掉导航栏「资产」与项目内资产/续费/评级相关内容（资产页、悬浮球、总览评级、续费提醒、小卡费用到期、迷你卡续费价格、资产相关设置）
- 流量页新增：往期记录（登录后可选今天至 6 天前，按当日窗口积分）与排序（节点/当日流量/上下行峰值）

## v1.2.5

- 修复「实时网速」排序未生效：原先 0.5/0.3 MB/s 的进出滞回门在低负载站点会让全部节点都不活跃、排序退化成默认权重序；改为所有在线节点参与排序（防抖由 3 样本滑动平均 + 5 秒重排承担）
- 主题设置新增「首页排序」：允许访客排序开关、默认排序字段（默认/名称/实时网速/累计流量/价格）、默认方向（升/降）

## v1.2.4

- 修复 COMPACT 模式卡片大小不统一：网格行等高（`grid-auto-rows: 1fr`）、标题最多两行超出省略、chip 行固定一行、底部 ping 区弹性填充
- 首页总览去掉「资产概览」卡（续费提醒入口随之移除；资产页 `/assets` 与悬浮球入口保留）

## v1.2.3

- LIST 表头每列之间加竖线分隔（不占布局、不干扰列宽拖拽手柄）
- 鼠标状态映射补全（设计来自 mouse.jpg 参考图）：忙碌/加载中 → 箭头+沙漏、滑杆与列宽手柄 → 水平调整、等待态 → 沙漏；与 automata.css 的整套 NieR 光标（逐像素提取自同张参考图）配合

## v1.2.2

- 修复 LIST 视图拖动列宽后样式错误：单元格补 `min-width: 0`（此前拖动收窄后内容溢出压到邻列）；末列改为弹性补位（`minmax(var(--lc9), 1fr)`），列宽总和小于视口时填满无空白尾、大于视口时横向滚动；表头内边距与数据行对齐；表头标签超出隐藏省略号

## v1.2.1

- 修复：排序控件现在始终靠右（`margin-left: auto`），下拉面板与按钮对齐，不再与地区栏错位重叠
- 深色模式再度调暗（画布 `#0e0d0a` / 面板 `#16140f` / 顶栏近黑 `#060504`）、文字提亮（`#e6dfc9`）、顶栏胶囊按钮在深色下显式米白——导航更清晰、整体亮度降低
- COMPACT 卡片长实例名换行显示（`overflow-wrap: anywhere`），不再超出边框
- LIST 视图：表头与数据行同模板对齐、各列居中、流量配额并排；**新增可拖拽列宽调整**（拖表头竖线手柄，宽度持久化到本机，窄屏按优先级隐藏网络/负载列）

## v1.2.0

修复卡片悬停可读性与底部黑色细线、调整深色模式（仅深色），并把 LuminaPlus 的全部剩余样式与功能融入：

- 修复：卡片悬停不再反转背景（黑底深字看不清）；恢复卡片四周黑色细线；深色模式仅调整暗色 token（画布/面板微调、网格线改米白、悬停描边改米白、theme-color 同步）
- 首页完整接入：总览四卡（含评级）、分组页签、地区筛选、排序控件、续费提醒、小卡/迷你卡/列表视图、资产悬浮球
- 详情页：实例切换器；小卡指标开关（累计流量/费用到期/在线时长/连接数）
- 顶栏配色选择器（指标色 + 暗色深度 + 保存到后端）
- 主题设置页补齐：总览评级、小卡与列表开关、模拟数据、隐藏节点、卡片不透明度、ADMIN 入口、Ping 图开关
- 视觉全部保持 NieR 风格（直角、米白/炭黑、等宽、`[ 标题 ]`、`--cur-*` 光标）

## v1.1.0

应用步骤写入 README，并把 LuminaPlus 的结构化样式（surface/home/node-card/cost-summary/traffic-stats）以 NieR 视觉语言合并进 `theme.css`：

- README 新增「发布到仓库 + 后台应用 + 自定义主题 URL 格式 + 注意事项」完整步骤
- 实例详情页（面板头/分段控件/开关/图表卡片/线路卡/丢包色带/空态）
- 首页（多线路行/线路切换菜单/柱状 tooltip）
- 流量页与资产页（汇总卡/明细表/账本/到期提醒/汇率）
- 主题设置页工具栏、错误页、实时连接时限浮条
- 视觉全部保持 NieR 风格：直角、米白/炭黑、等宽数值、`[ 标题 ]`、`--cur-*` 光标

## v1.0.0

NieR:Automata Bunker Terminal 风格外壳 + LuminaPlus 数据层移植（持续更新：本段描述随每次发布同步到当前状态）。

- NieR 设计系统（配色 / 全套光标 / 面板 / 顶栏 / 页脚 / 深色模式 / 内置品牌图标）
- 性能：卡片 content-visibility 懒渲染、详情/流量明细懒加载、图标压缩（2KB）
- 首页：总览卡 + 大/小/迷你/列表四档卡片（含 Swap 栏、实时网速、延迟/丢包柱、多线路切换、整卡点击详情、统一尺寸）+ 分组/地区筛选 + 排序（默认/名称/实时网速/累计流量）
- 详情页：实例切换器、信息面板、负载图表（uPlot）与 Ping 图表（丢包色带/削峰/断点）
- 流量页：当日与往期（日历选择、登录 7 天）、实时带宽、可排序表格、速率明细图
- 主题设置页：外观/视图/网格列数/分组顺序拖动/表格列开关/线路/排序等，保存到本机/后端/复制 JSON
- 顶栏：外观三键、视图下拉、内置配色器（预设色板 + HEX）、语言切换、移动端底部导航
- 中/英双语；数据层（services/hooks/utils/types）与测试全套
